/**
 * @param {string} s
 * @return {number}
 */
var minDeletions = function(s) {
	let chars = new Set(s)
	uniqusCount = chars.size
	//calc freq of uniqus


 };

 let test= minDeletions("aaab")
 console.log(test);